specVersion: "v1beta"
serviceId: "blog-starter-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-starter-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
